# Build customization
build:
  main: main.go httplog.go ioskit.go main.go revproxy.go
  flags: -tags vfs
  goarch:
    - amd64
  goos:
    - darwin
brew:
  repo: openatx/homebrew-tap
  folder: Formula
  dependencies:
    - libimobiledevice
