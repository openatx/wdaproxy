# Build customization
build:
  main: .
  flags: -tags vfs
  goarch:
    - amd64
  goos:
    - darwin
brew:
  github: 
    owner: openatx
    name: homebrew-tap
  folder: Formula
  dependencies:
    - libimobiledevice
